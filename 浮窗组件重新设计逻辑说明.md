浮窗组件重新设计逻辑说明
一、整体结构统一
原设计三个状态各自为政（按钮堆砌、卡片形式不一致），重新设计统一为一套固定骨架，只是内容区随状态变化：
[×]                    ← 右上角关闭按钮，固定位置，三态统一,另外点击任何非组件区域都应该会触发关闭。
词头 [书签图标]           ← 标题行，词+标记状态图标同排
(音标 / 词性标签)         ← 仅详情态显示
─────────────────       ← 分隔线
[内容区 —— 随状态变化]
─────────────────
[发音] [从本句开始朗读全文]  ← 底部工具栏，固定不变，三态统一
逻辑依据：用户在三个状态之间切换时，眼睛不需要重新寻找"关闭在哪""朗读在哪"——固定位置的元素越多，操作记忆负担越小。
二、关闭按钮

位置：从"和其他按钮并排"改为浮窗右上角绝对定位，脱离内容流。
原因：关闭是唯一一个"退出型"操作，理应和其他功能性操作（标记、发音）在视觉上分离，避免误触，也符合大多数弹层/模态框的通用交互习惯（右上角×是用户已有的肌肉记忆）。

三、书签图标（标记生词）从"关闭按钮旁"移到"词头旁"

原因：标记生词是和"这个词"直接相关的操作，语义上应该贴着词本身，而不是贴着"关闭"这个无关操作。
交互：图标本身承担"开关"语义——未标记为描边图标，已标记为实心图标+浅色背景高亮，点击可直接切换，不需要额外弹提示。

四、书签角标（丝带）—— 新增的状态提示

触发条件：仅当词已被标记为生词时出现（状态二、三）。
位置：卡片左上角（为了不和右上角的关闭按钮打架）。
原因：给"已标记"状态一个比纯文字更快识别的视觉信号，类似书本里夹的书签，呼应"生词本"这个产品概念，同时不占用内容区空间。

五、"标记生词"与"加入记事本"的语义拆分
原设计里两个概念容易混在一起，重新设计明确区分：
操作位置含义标记生词（书签图标）词头旁的图标轻量、即时的"这个词我不认识"标记加入记事本状态二的主按钮更正式的"归档到 Learners 词库"动作取消本篇标记状态二的次要文字按钮撤销性质，故意做得不显眼（低对比度文字按钮），避免和主按钮抢注意力
六、"从本句开始全文朗读"的位置调整

改动：从"只出现在状态一"改为在三个状态的底部工具栏里都固定存在，和"发音"并列。
原因：这是一个和当前所在句子相关的通用能力，不应该因为用户进入了"标记确认"或"查看详情"状态就消失——用户可能在看完词义详情后，仍然想直接开始跟读这句话。

七、字体与排版

词头用衬线字体（Source Serif 4 / 思源宋体思路），其余 UI 文字用无衬线字体。
原因：衬线字体天然带"词典感"，能让"词"本身在视觉上和周围的功能性按钮/说明文字区分开，不需要额外加粗或变色就能形成层次。

八、颜色

保留原来的琥珀色主色调（品牌延续性），但：

降低了背景/边框的饱和度，避免大面积高饱和色造成阅读疲劳
只有"主按钮"（标记为生词、加入记事本）用实心琥珀色，其余按钮统一用描边/幽灵按钮，确保每个状态里视觉焦点只有一个
状态提示条（"已标记为生词"）用浅琥珀色底+深琥珀色字，和主按钮的"实色"区分开，避免两者抢视觉权重

---以下是代码参考---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<title>查词浮窗 · 重新设计</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Source+Serif+4:ital,wght@0,500;0,600;1,500&family=Noto+Serif+SC:wght@600&family=Inter:wght@400;500;600&family=Noto+Sans+SC:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root{
    --paper:#F7F2E9;
    --card:#FFFFFF;
    --ink:#2A2118;
    --ink-muted:#8C7E6C;
    --line:#E7DCC7;
    --amber:#A2621E;
    --amber-deep:#6F4310;
    --amber-tint:#F4E4C6;
    --amber-tint-strong:#EAD09B;
    --red-muted:#9C4A3C;
    --serif:'Source Serif 4','Noto Serif SC',Georgia,serif;
    --sans:'Inter','Noto Sans SC',-apple-system,'PingFang SC',system-ui,sans-serif;
  }
  *{box-sizing:border-box;}
  body{
    margin:0;
    background:var(--paper);
    font-family:var(--sans);
    color:var(--ink);
    padding:40px 20px 80px;
    display:flex;
    flex-direction:column;
    align-items:center;
  }

  /* ---- demo chrome (not part of the real component) ---- */
  .demo-label{
    font-size:12px;
    color:var(--ink-muted);
    letter-spacing:.06em;
    margin-bottom:10px;
    text-transform:uppercase;
  }
  .switcher{
    display:flex;
    gap:6px;
    background:var(--card);
    border:1px solid var(--line);
    border-radius:10px;
    padding:4px;
    margin-bottom:36px;
  }
  .switcher button{
    border:none;
    background:transparent;
    padding:8px 14px;
    font-family:var(--sans);
    font-size:13px;
    font-weight:500;
    color:var(--ink-muted);
    border-radius:7px;
    cursor:pointer;
    transition:background .15s ease,color .15s ease;
  }
  .switcher button.active{
    background:var(--amber-tint);
    color:var(--amber-deep);
  }

  .stage{
    width:100%;
    max-width:640px;
    background:var(--card);
    border:1px solid var(--line);
    border-radius:14px;
    padding:44px 48px 90px;
    position:relative;
  }
  .passage{
    font-family:var(--serif);
    font-size:17px;
    line-height:1.9;
    color:var(--ink);
    max-width:480px;
  }
  .target-word{
    position:relative;
    color:var(--amber-deep);
    font-weight:600;
    text-decoration:underline;
    text-decoration-color:var(--amber-tint-strong);
    text-decoration-thickness:3px;
    text-underline-offset:3px;
  }

  /* ---- the actual popup component ---- */
  .popup{
    position:absolute;
    top:150px;
    left:48px;
    width:296px;
    background:var(--card);
    border:1px solid var(--line);
    border-radius:12px;
    box-shadow:0 10px 28px rgba(60,42,12,.14), 0 2px 6px rgba(60,42,12,.06);
    padding:16px 18px 14px;
  }
  .popup[hidden]{display:none;}

  /* bookmark ribbon — signature element, shown only once a word is marked */
  .ribbon{
    position:absolute;
    top:-2px;
    left:22px;
    width:18px;
    height:26px;
    background:var(--amber);
    clip-path:polygon(0 0,100% 0,100% 100%,50% 78%,0 100%);
  }

  .close-btn{
    position:absolute;
    top:10px;
    right:10px;
  }

  .popup-head{
    padding-right:30px;
  }
  .word-block{display:flex;flex-direction:column;gap:2px;}
  .word-row{
    display:flex;
    align-items:center;
    gap:6px;
  }
  .word{
    font-family:var(--serif);
    font-size:19px;
    font-weight:600;
    color:var(--ink);
    line-height:1.2;
  }
  .phonetic{
    font-family:var(--serif);
    font-style:italic;
    font-size:12.5px;
    color:var(--ink-muted);
  }
  .pos-tags{display:flex;gap:5px;margin-top:2px;}
  .pos-tag{
    font-size:10.5px;
    font-weight:600;
    color:var(--amber-deep);
    background:var(--amber-tint);
    border-radius:4px;
    padding:1px 6px;
    font-family:var(--sans);
  }

  .icon-btn{
    width:28px;height:28px;
    border-radius:7px;
    border:1px solid transparent;
    background:transparent;
    display:flex;align-items:center;justify-content:center;
    cursor:pointer;
    color:var(--ink-muted);
    flex-shrink:0;
    transition:background .15s ease,color .15s ease,border-color .15s ease;
  }
  .icon-btn:hover{background:var(--paper);border-color:var(--line);}
  .icon-btn svg{width:15px;height:15px;}
  .icon-btn.bookmark.active{color:var(--amber-deep);background:var(--amber-tint);}
  .head-actions{display:flex;gap:4px;}

  .divider{
    height:1px;background:var(--line);
    margin:12px 0;
  }

  .body-text{
    font-size:13px;
    color:var(--ink-muted);
    line-height:1.6;
    margin:0 0 12px;
  }

  .status-row{
    display:flex;
    align-items:center;
    gap:8px;
    background:var(--amber-tint);
    border-radius:8px;
    padding:8px 10px;
    margin-bottom:12px;
  }
  .status-row svg{width:14px;height:14px;color:var(--amber-deep);flex-shrink:0;}
  .status-row .status-main{font-size:12.5px;font-weight:500;color:var(--amber-deep);}
  .status-row .status-sub{font-size:11px;color:var(--ink-muted);margin-top:1px;}

  .definition{
    font-size:13px;
    line-height:1.75;
    color:var(--ink);
    margin:0 0 10px;
  }
  .definition b{color:var(--amber-deep);font-weight:600;}

  .example{
    font-family:var(--serif);
    font-style:italic;
    font-size:13px;
    color:var(--ink-muted);
    border-left:2px solid var(--amber-tint-strong);
    padding:2px 0 2px 11px;
    margin:0 0 14px;
  }

  .btn{
    font-family:var(--sans);
    font-size:13px;
    font-weight:500;
    border-radius:8px;
    padding:9px 12px;
    cursor:pointer;
    display:flex;align-items:center;justify-content:center;gap:6px;
    border:1px solid var(--line);
    background:var(--card);
    color:var(--ink);
    transition:background .15s ease,border-color .15s ease;
  }
  .btn:hover{background:var(--paper);}
  .btn svg{width:14px;height:14px;}
  .btn-primary{
    background:var(--amber);
    border-color:var(--amber);
    color:#FBF3E7;
  }
  .btn-primary:hover{background:var(--amber-deep);border-color:var(--amber-deep);}
  .btn-muted-text{
    border-color:transparent;background:transparent;color:var(--ink-muted);
  }
  .btn-muted-text:hover{background:var(--paper);color:var(--red-muted);}

  .action-row{display:flex;gap:8px;margin-bottom:10px;}
  .action-row .btn{flex:1;}

  .utility-row{
    display:flex;gap:8px;
    padding-top:10px;
    border-top:1px solid var(--line);
  }
  .utility-row .btn{flex:1;font-size:12.5px;padding:8px 8px;color:var(--ink-muted);}
  .utility-row .btn:hover{color:var(--ink);}
  .utility-row .btn.pulse svg{color:var(--amber-deep);}

  .caption{
    text-align:center;
    font-size:12px;
    color:var(--ink-muted);
    margin-top:14px;
  }
</style>
</head>
<body>

<div class="demo-label">同一浮窗组件 · 三种状态演示（点击上方切换）</div>
<div class="switcher">
  <button class="active" data-state="select">划词选中</button>
  <button data-state="marked">标记生词后</button>
  <button data-state="detail">查看词义详情</button>
</div>

<div class="stage">
  <p class="passage">
    Reading a little every day is the simplest way to grow your vocabulary.
    Consistent <span class="target-word">practice</span>, even just ten minutes,
    compounds over weeks into real fluency.
  </p>

  <!-- 状态一：划词选中，等待操作 -->
  <div class="popup" id="popup-select">
    <button class="icon-btn close-btn" aria-label="关闭">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
    <div class="popup-head">
      <div class="word-block">
        <div class="word-row">
          <span class="word">practice</span>
          <button class="icon-btn bookmark" aria-label="标记生词" onclick="toggleBookmark(this)">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M6 3h12v18l-6-4.5L6 21V3z"/></svg>
          </button>
        </div>
      </div>
    </div>
    <div class="divider"></div>
    <div class="action-row">
      <button class="btn btn-primary">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M6 3h12v18l-6-4.5L6 21V3z"/></svg>
        标记为生词
      </button>
    </div>
    <div class="utility-row">
      <button class="btn">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M11 5 6 9H2v6h4l5 4V5z"/><path d="M15.5 8.5a5 5 0 010 7"/></svg>
        发音
      </button>
      <button class="btn">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="12" cy="12" r="9"/><path d="M10 8.5l6 3.5-6 3.5v-7z"/></svg>
        从本句开始朗读全文
      </button>
    </div>
  </div>

  <!-- 状态二：已标记为生词 -->
  <div class="popup" id="popup-marked" hidden>
    <div class="ribbon"></div>
    <button class="icon-btn close-btn" aria-label="关闭">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
    <div class="popup-head">
      <div class="word-block">
        <div class="word-row">
          <span class="word">practice</span>
          <button class="icon-btn bookmark active" aria-label="取消标记" onclick="toggleBookmark(this)">
            <svg viewBox="0 0 24 24" fill="currentColor"><path d="M6 3h12v18l-6-4.5L6 21V3z"/></svg>
          </button>
        </div>
      </div>
    </div>
    <div class="divider"></div>
    <div class="status-row">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 13l4 4L19 7"/></svg>
      <div>
        <div class="status-main">已标记为生词</div>
        <div class="status-sub">来自 Learners 词库</div>
      </div>
    </div>
    <div class="action-row">
      <button class="btn btn-primary">加入记事本</button>
      <button class="btn btn-muted-text">取消本篇标记</button>
    </div>
    <div class="utility-row">
      <button class="btn">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M11 5 6 9H2v6h4l5 4V5z"/><path d="M15.5 8.5a5 5 0 010 7"/></svg>
        发音
      </button>
      <button class="btn">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="12" cy="12" r="9"/><path d="M10 8.5l6 3.5-6 3.5v-7z"/></svg>
        从本句开始朗读全文
      </button>
    </div>
  </div>

  <!-- 状态三：词义详情 -->
  <div class="popup" id="popup-detail" hidden>
    <button class="icon-btn close-btn" aria-label="关闭">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M6 6l12 12M18 6L6 18"/></svg>
    </button>
    <div class="popup-head">
      <div class="word-block">
        <div class="word-row">
          <span class="word">practice</span>
          <button class="icon-btn bookmark active" aria-label="取消标记" onclick="toggleBookmark(this)">
            <svg viewBox="0 0 24 24" fill="currentColor"><path d="M6 3h12v18l-6-4.5L6 21V3z"/></svg>
          </button>
        </div>
        <div class="phonetic">/ˈpræktɪs/</div>
        <div class="pos-tags"><span class="pos-tag">v.</span><span class="pos-tag">n.</span></div>
      </div>
    </div>
    <div class="divider"></div>
    <p class="definition"><b>v.</b> 实践，练习，实习，习，推行，演</p>
    <p class="definition" style="margin-top:-6px;"><b>n.</b> 练习，做法，实际，作法，习惯，操练，演习，熟练</p>
    <p class="example">"Learners practice daily."</p>
    <div class="utility-row">
      <button class="btn">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M11 5 6 9H2v6h4l5 4V5z"/><path d="M15.5 8.5a5 5 0 010 7"/></svg>
        发音
      </button>
      <button class="btn">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="12" cy="12" r="9"/><path d="M10 8.5l6 3.5-6 3.5v-7z"/></svg>
        从本句开始朗读全文
      </button>
    </div>
  </div>
</div>

<div class="caption">amber 主色 + 衬线词头 + 书签角标：统一三态浮窗，朗读入口固定常驻</div>

<script>
  const buttons = document.querySelectorAll('.switcher button');
  const popups = {
    select: document.getElementById('popup-select'),
    marked: document.getElementById('popup-marked'),
    detail: document.getElementById('popup-detail'),
  };
  buttons.forEach(btn=>{
    btn.addEventListener('click', ()=>{
      buttons.forEach(b=>b.classList.remove('active'));
      btn.classList.add('active');
      Object.values(popups).forEach(p=>p.hidden = true);
      popups[btn.dataset.state].hidden = false;
    });
  });
  function toggleBookmark(el){
    el.classList.toggle('active');
  }
</script>

</body>
</html>
